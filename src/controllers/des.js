const Enkripsi_des = (text, key) => {
	let result = '';
	// logic untuk enkripsi data
	return result
}

const Deskripsi_des = (encryptedData, key) => {
	let result = '';
	// logic untuk dekripsi data yg dienkripsi
	return result
}

const generateDesKey = () => {
	// ini diisi logic buat nentuin key des
}

module.exports = { Enkripsi_des, Deskripsi_des, generateDesKey }